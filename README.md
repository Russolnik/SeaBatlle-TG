# 🎮 Telegram-бот «Морской бой»

Полнофункциональный Telegram-бот для игры в «Морской бой» с полным UI на инлайн-кнопках.

## ✨ Особенности

- 🎯 Полностью на инлайн-кнопках (без изображений)
- ⚓ Ручная расстановка кораблей с перемещением и поворотом
- 🎲 Автоматическая расстановка кораблей
- ⚔️ Бой через инлайн-кнопки
- 📢 Уведомления о попаданиях и уничтожениях
- 🏆 Финальный отчёт с раскрытием полей
- ⚔️ Реванш с автоматическим приглашением
- 🎮 Два режима: Обычный (8×8) и Быстрый (6×6)
- ⏱ Таймер на ход (1 мин для быстрого, 2 мин для обычного)
- 📊 Информационное табло с последним ходом
- 🌊 Отображение атак противника на своем поле

## 🚀 Установка и запуск

### Требования

- Python 3.8+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

### Локальная установка

1. Клонируйте репозиторий:

```bash
git clone https://github.com/Russolnik/SeaBatlle-TG.git
cd SeaBatlle-TG
```

2. Установите зависимости:

```bash
pip install -r requirements.txt
```

3. Создайте файл `.env` в корне проекта (скопируйте `.env.example`):

```bash
cp .env.example .env
```

4. Отредактируйте `.env` и укажите ваши переменные окружения:

```env
# Обязательные переменные
BOT_TOKEN=your_telegram_bot_token

# Опциональные переменные
TELEGRAM_API=https://api.telegram.org
BACKEND_URL=https://seabatlle-tg.onrender.com
WEBAPP_URL=https://your-app.netlify.app
PORT=5000
```

5. Запустите бота:

```bash
python bot.py
```

### Деплой на Render

1. Создайте аккаунт на [Render.com](https://render.com)
2. Подключите ваш GitHub репозиторий
3. Создайте новый **Web Service**
4. Настройки:
   - **Environment**: Python 3
   - **Python Version**: `3.11` (важно для совместимости)
   - **Build Command**: `pip install --upgrade pip setuptools wheel && pip install -r requirements.txt`
   - **Start Command**: `python bot.py`
5. Добавьте переменные окружения:
   - `BOT_TOKEN` - ваш токен бота (обязательно)
   - `TELEGRAM_API` (опционально) - кастомный API endpoint
   - `BACKEND_URL` (опционально) - URL вашего бэкенда для CORS, по умолчанию `https://seabatlle-tg.onrender.com`
   - `WEBAPP_URL` (опционально) - URL веб-приложения для кнопки Mini App
6. Нажмите **Create Web Service**

**Важно**: На Render используйте тип сервиса **Web Service**, но бот будет работать как worker (это нормально для Telegram ботов).

## 📖 Использование

### Создание игры

1. Отправьте команду `/play` (можно в личных сообщениях или в группе)
2. Выберите режим игры (Обычный или Быстрый)
3. Выберите, нужен ли таймер
4. Отправьте ссылку другу или в группу для присоединения

### Расстановка кораблей

- Используйте кнопки **← → ↑ ↓** для перемещения корабля
- Нажмите **↻ Повернуть** для изменения ориентации
- Нажмите **✅ Установить** для размещения корабля
- Или используйте **🎲 Авто** для автоматической расстановки
- Нажмите **✅ Готово**, когда все корабли расставлены

### Бой

- Нажимайте на клетки поля противника (`🌊`) для атаки
- Следите за индикатором хода
- Используйте **🚩 Сдаться** для завершения игры

### После игры

- **🎮 Создать новую игру** - создаёт новую игру в группе
- **⚔️ Реванш** - отправляет приглашение противнику на реванш

## 🎨 Обозначения

- `🌊` - море (пустая клетка)
- `🟥` - ваш корабль (видно только вам)
- `🟦` - призрачный корабль (при расстановке, если валидно)
- `❌` - призрачный корабль (при расстановке, если невалидно) / уничтоженный корабль
- `⚫` - мимо / заблокированная клетка вокруг уничтоженного корабля
- `🔥` - попадание

## 📁 Структура проекта

```
.
├── bot.py              # Основной файл бота
├── models.py           # Модели данных (GameState, Player)
├── game_logic.py       # Логика игры (валидация, атаки, расстановка)
├── keyboards.py        # Клавиатуры для интерфейса
├── requirements.txt    # Зависимости Python
├── .env.example        # Пример конфигурации
├── .gitignore          # Игнорируемые файлы для Git
├── render.yaml         # Конфигурация для деплоя на Render
├── Procfile            # Конфигурация процесса для Render
└── README.md           # Документация
```

## 🔧 Конфигурация

### Режимы игры

**Обычный (8×8):**
- 2 корабля на 3 клетки
- 2 корабля на 2 клетки
- 4 корабля на 1 клетку
- **Таймер**: 2 минуты на ход (если включен)

**Быстрый (6×6):**
- 1 корабль на 3 клетки
- 1 корабль на 2 клетки
- 2 корабля на 1 клетку
- **Таймер**: 1 минута на ход (если включен)

### Правила размещения

- Корабли не должны соприкасаться (минимум 1 клетка между ними, включая диагонали)
- Корабли не должны выходить за границы поля
- Корабли не должны пересекаться

## 🐛 Известные ограничения

- Игры хранятся в памяти (при перезапуске бота игры теряются)
- Статистика пока не реализована

## 📦 Деплой

### Render.com

Проект готов для деплоя на Render. Используйте файл `render.yaml` или настройте вручную:

1. Подключите GitHub репозиторий
2. Выберите тип: **Web Service**
3. Build Command: `pip install -r requirements.txt`
4. Start Command: `python bot.py`
5. Добавьте переменные окружения:
   - `BOT_TOKEN` - токен бота (обязательно)
   - `BACKEND_URL` - URL бэкенда для CORS (опционально)
   - `WEBAPP_URL` - URL веб-приложения (опционально)

### Другие платформы

Бот также можно задеплоить на:
- Heroku (используйте `Procfile`)
- Railway
- VPS с systemd
- Docker (создайте Dockerfile)

## 📝 Лицензия

Проект создан в образовательных целях.

## 🤝 Вклад

Приветствуются улучшения и исправления!

