# 🚀 Инструкция по деплою на Render

## Шаг 1: Подготовка репозитория

1. Убедитесь, что все файлы закоммичены и запушены в GitHub:
```bash
git add .
git commit -m "Initial commit"
git push origin main
```

## Шаг 2: Создание сервиса на Render

1. Зайдите на [render.com](https://render.com) и войдите через GitHub
2. Нажмите **New +** → **Web Service**
3. Подключите репозиторий `Russolnik/SeaBatlle-TG`

## Шаг 3: Настройка сервиса

### Основные настройки:
- **Name**: `seabattle-telegram-bot` (или любое другое)
- **Environment**: `Python 3`
- **Region**: выберите ближайший к вам
- **Branch**: `main` (или ваша основная ветка)

### Build & Deploy:
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `python bot.py`

### Environment Variables:
Добавьте переменные окружения:
- `BOT_TOKEN` - ваш токен бота от @BotFather
- `TELEGRAM_API` (опционально) - по умолчанию `https://api.telegram.org`

## Шаг 4: Деплой

1. Нажмите **Create Web Service**
2. Render начнет сборку и деплой
3. Дождитесь завершения (обычно 2-3 минуты)
4. Проверьте логи - бот должен запуститься

## Шаг 5: Проверка работы

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start` или `/play`
3. Если бот отвечает - деплой успешен! 🎉

## 🔧 Решение проблем

### Бот не отвечает
- Проверьте логи в Render Dashboard
- Убедитесь, что `BOT_TOKEN` установлен правильно
- Проверьте, что бот не заблокирован

### Ошибки при сборке
- Убедитесь, что `requirements.txt` содержит все зависимости
- Проверьте версию Python (должна быть 3.8+)

### Бот падает после запуска
- Проверьте логи на наличие ошибок
- Убедитесь, что все переменные окружения установлены

## 📝 Примечания

- Render предоставляет бесплатный план с ограничениями
- Бот будет "засыпать" после 15 минут бездействия на бесплатном плане
- Для постоянной работы рассмотрите платный план или другой хостинг

## 🔄 Обновление бота

После каждого push в GitHub, Render автоматически пересоберет и задеплоит новую версию.

