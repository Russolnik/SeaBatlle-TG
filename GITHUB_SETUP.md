# 📤 Инструкция по загрузке на GitHub

## Шаг 1: Инициализация Git репозитория

Если Git еще не инициализирован, выполните:

```bash
git init
```

## Шаг 2: Добавление всех файлов

```bash
git add .
```

## Шаг 3: Создание первого коммита

```bash
git commit -m "Initial commit: Sea Battle Telegram Bot"
```

## Шаг 4: Подключение к GitHub

1. Убедитесь, что репозиторий `SeaBatlle-TG` создан на GitHub (он уже существует, но пустой)
2. Добавьте remote:

```bash
git remote add origin https://github.com/Russolnik/SeaBatlle-TG.git
```

## Шаг 5: Загрузка на GitHub

```bash
git branch -M main
git push -u origin main
```

Если потребуется авторизация, GitHub может запросить:
- Personal Access Token (рекомендуется)
- Или используйте GitHub Desktop / GitHub CLI

## Шаг 6: Создание .env.example

После загрузки на GitHub, создайте файл `.env.example` через веб-интерфейс GitHub или локально:

**Содержимое `.env.example`:**
```
# Telegram Bot Token (обязательно)
# Получите у @BotFather в Telegram
BOT_TOKEN=your_bot_token_here

# Telegram Bot API (опционально)
# По умолчанию: https://api.telegram.org
# Используйте для прокси или кастомного сервера
TELEGRAM_API=https://api.telegram.org
```

## ✅ Проверка

После загрузки проверьте, что на GitHub есть все файлы:
- ✅ bot.py
- ✅ models.py
- ✅ game_logic.py
- ✅ keyboards.py
- ✅ requirements.txt
- ✅ README.md
- ✅ .gitignore
- ✅ render.yaml
- ✅ Procfile
- ✅ DEPLOY.md
- ✅ .env.example (создайте вручную)

## 🚀 После загрузки

Теперь можно переходить к деплою на Render (см. DEPLOY.md)

