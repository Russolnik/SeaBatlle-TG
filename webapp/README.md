# Морской бой - Telegram Mini App

Веб-приложение для игры "Морской бой" в формате Telegram Mini App с полем 10×10.

## 🚀 Быстрый старт

### Локальная разработка

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env.local`:
```env
VITE_API_URL=http://localhost:5000
VITE_WS_URL=http://localhost:5000
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📦 Деплой на Netlify

См. подробную инструкцию в файле [NETLIFY_DEPLOY.md](./NETLIFY_DEPLOY.md)

### Краткая инструкция:

1. Загрузите код в GitHub
2. Войдите в [Netlify](https://app.netlify.com)
3. Импортируйте репозиторий
4. Настройки:
   - **Base directory**: `webapp`
   - **Build command**: `npm run build`
   - **Publish directory**: `webapp/dist`
5. Добавьте переменные окружения:
   - `VITE_API_URL` = URL вашего бэкенда
   - `VITE_WS_URL` = WebSocket URL (wss:// для HTTPS)

## 🎮 Особенности

- ✅ Поле 10×10 с подписями координат (A-J, 1-10)
- ✅ Адаптивный дизайн (мобильные и десктоп)
- ✅ Темная/светлая тема (автоматически по системным настройкам)
- ✅ Реалтайм обновления через WebSocket
- ✅ Авторизация через Telegram WebApp
- ✅ Оптимизация для слабых устройств
- ✅ Режим 10×10 с полным набором кораблей (1×4, 2×3, 3×2, 4×1)

## 📁 Структура проекта

```
webapp/
├── src/
│   ├── components/      # React компоненты
│   │   ├── Board.jsx   # Игровое поле 10×10
│   │   ├── GameBoard.jsx
│   │   ├── GameSetup.jsx
│   │   ├── GameLobby.jsx
│   │   └── GameInfo.jsx
│   ├── hooks/          # React хуки
│   │   ├── useTelegramAuth.js
│   │   └── useWebSocket.js
│   ├── utils/          # Утилиты
│   │   └── api.js
│   ├── App.jsx         # Главный компонент
│   ├── main.jsx        # Точка входа
│   └── index.css       # Глобальные стили
├── public/
│   └── _redirects     # Редиректы для Netlify
├── index.html
├── netlify.toml        # Конфигурация Netlify
├── package.json
└── vite.config.js
```

## 🔧 Технологии

- **React 18** - UI библиотека
- **Vite** - Сборщик и dev-сервер
- **Tailwind CSS** - Стилизация
- **Socket.IO Client** - WebSocket для реалтайм обновлений
- **Telegram WebApp SDK** - Интеграция с Telegram

## 📝 Переменные окружения

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `VITE_API_URL` | URL бэкенда API | `https://your-backend.com` |
| `VITE_WS_URL` | URL WebSocket | `wss://your-backend.com` |

## 🐛 Решение проблем

### localhost:3000 не работает

1. Убедитесь, что dev-сервер запущен: `npm run dev`
2. Проверьте, что порт 3000 свободен
3. Попробуйте другой порт: измените `port: 3000` в `vite.config.js`

### Ошибки подключения к API

1. Проверьте, что бэкенд запущен на `localhost:5000`
2. Проверьте переменные окружения в `.env.local`
3. Убедитесь, что CORS настроен на бэкенде

### WebSocket не подключается

1. Проверьте, что используется правильный протокол (ws:// для HTTP, wss:// для HTTPS)
2. Убедитесь, что Flask-SocketIO запущен на бэкенде
3. Проверьте переменную `VITE_WS_URL`
